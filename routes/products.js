const express = require('express');
const router = express.Router();

// Product listing
router.get('/', (req, res) => {
  // Get all products
});

// Product details
router.get('/:id', (req, res) => {
  // Get product by ID
});

module.exports = router;
